/**
 * Theme.js is where we store theme based styles, background color, global padding, text sizes
 */
import { StyleSheet } from 'react-native';
import cairn from 'cairn';

export default cairn({
}, (styles) => StyleSheet.create(styles));
